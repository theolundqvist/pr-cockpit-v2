<!-- orchestrate handoff
task: m3-verify
branch: `orch/m3-diff-worktree-notifs/m3-finalize-merge` (verifier artifacts committed at `9ce9751`)
agentId: bc-48d32ffb-ae11-4c3f-aaa8-068dac8be8c3
runId: run-f1b3aebc-4fc5-4621-b67a-2e9148b1bff6
resultStatus: finished
finishedAt: 2026-05-18T00:03:42.760Z
-->

AddReviewComment is verified — it is a `Mutation` impl with `OptimismLevel::Full` and the GraphQL artifact is at `apps/desktop/src-tauri/src/api/queries/mutations/addReviewComment.graphql`. Let me check the canonical-queries guard, viewed checkbox state, asset protocol scope, and other key invariants.Viewed checkbox state is head_sha-scoped via the read-model `CASE WHEN pf.viewed_by_account_id IS NOT NULL AND pf.viewed_at_head_sha = pr.head_sha THEN 1` SQL. Let me check the tauri.conf.json + capabilities for asset protocol scope, then the worktree watcher and notification dedup.Asset protocol scope is tightly limited to `$APPDATA/blobs/**/*`. Let me check the worktree watcher and notification system.Worktree watcher verified: `notify` (RecommendedWatcher), 200-500 ms debounce (`debounce_delay_for` → `200 + (hasher.finish() % 301)` = 200..500ms), `git status --porcelain=v2 --branch`, `git rev-list --left-right --count`. Now let me check notification dedup and cleanup safety.Notification dedup verified: UNIQUE constraint on `(account_id, repo_id, pr_id, event_type, actor_id, server_event_id)` + `INSERT OR IGNORE` pattern. Now let me check worktree cleanup safety.Now check the renderer isolation test and corpus tightening.Renderer doesn't call notification plugin directly. Now let me check the corpus tightening and verify the gate is 0.015 not 0.02.Good — `pnpm corpus` uses `score.mjs` (gate=0.015). Now let me set up the environment and run the build.Need to install Tauri Linux deps + playwright browsers. Let me kick this off.Both done. Now let me run the cargo checks while installing playwright browsers.fmt passes. Let me run clippy + test in parallel.Playwright installed. Now let me wait for clippy.Clippy passes. Now run cargo test in parallel with pnpm checks.Cargo test passed. Let me see all summary lines.Cargo test: 73 tests passed, 0 failed. Now check pnpm-checks status.All pnpm checks passed (typecheck, svelte-check, lint, 9 vitest tests). Now run bench + corpus + Playwright.Corpus passes — `entries=202`, `weighted_mean=0.000115 ≤ gate=0.015`. Now bench.All bench budgets pass. `comrak_render_throughput_ops_per_sec=36564.14` ≥ 15000, `mutation_submit_visible_ms=0.31` < 16, `diff_scroll_frame_p95_ms=16.20` ≤ 16.7. Now run Playwright suites.While Playwright runs, let me verify the token leak grep, DECISIONS.md, and explore the IPC bindings.Renderer doesn't call notification plugin. Now check DECISIONS.md.DECISIONS.md is consolidated with the `## M3 contract decisions (promoted for M4+)` block at the top. Now let me check the Playwright status.All 7 Playwright tests passed. Let me verify the accessibility spec content.Rename detection is present. Now check the M3 smoke artifacts and IPC bindings.All 5 smoke screenshots present. Let me make sure the IPC bindings include the new worktree+notif commands.Both bookkeeping branches are pinned to the same HEAD. Let me check the IPC bindings test and write the verifier artifacts.Now let me create the verifier artifacts directory and write the verdict.## Verification
live-ui-verified

## Target
`m3-finalize-merge` on branch `orch/m3-diff-worktree-notifs/m3-finalize-merge`

## Branch
`orch/m3-diff-worktree-notifs/m3-finalize-merge` (verifier artifacts committed at `9ce9751`)

## Execution
- `cargo fmt --check` → pass.
- `cargo clippy --workspace --all-targets -- -D warnings` → pass (compiled in 4m48s, no warnings).
- `cargo test --workspace` → 73 passed / 0 failed (full log at `verify/m3/cargo_test.log`).
- `pnpm ipc:bindings && pnpm typecheck && pnpm svelte-check && pnpm lint && pnpm test` → all pass (svelte-check: 0 errors / 0 warnings; vitest: 9 passed; ESLint + Prettier clean).
- `pnpm bench` → all PLAN.md §10 budgets green (`verify/m3/bench.log`).
- `pnpm corpus` → `entries=202`, `weighted_mean=0.000115`, `gate=0.015`.
- `xvfb-run -a pnpm exec playwright test airplane.spec.ts m2-smoke.spec.ts diff-polish.spec.ts worktree.spec.ts notifications.spec.ts a11y-pr-detail.spec.ts m3-smoke.spec.ts` → 7 passed / 0 failed (`verify/m3/playwright.log`).
- Source audits: AddReviewComment + viewed_at_head_sha SQL + tauri.conf.json/capabilities + worktree watcher + cleanup safety + notification dedup + renderer-no-plugin grep + token-leak grep.

## Findings
Per acceptance criterion:
- [x] All four sibling branches merged with `--no-ff` commits and explicit messages: confirmed via `git log --oneline` (commits `116e2d5` diff-polish, `ae25c48` worktree, `3ac65d9` notifications, `1f4ef90` corpus-tighten); each is a merge commit with `orchestrate(m3): merge <slice> …` message. **met**
- [x] Full local CI matrix passes (fmt/clippy/test, typecheck/svelte-check/lint/test/bench/corpus, Playwright airplane + m2-smoke + diff-polish + worktree + notifications + a11y + m3-smoke): all reproduced green from a fresh shell. **met**
- [x] `pnpm corpus` reports `weighted_mean ≤ 0.015` on 202 entries and the scorer gate is `0.015`: observed `entries=202`, `weighted_mean=0.000115`, `gate=0.015` (scorer at `tools/markdown-corpus/score.mjs:12` is `0.015`). **met**
- [x] End-to-end smoke captured under `artifacts/m3-smoke/`: `01-multiline-suggestion-compose.png` through `05-notification-trigger-simulations.png` present; produced by `playwright/m3-smoke.spec.ts`. **met**
- [x] Accessibility screen-reader Playwright spec exists and passes: `apps/desktop/playwright/a11y-pr-detail.spec.ts` enforces aria/visible-text for every interactive element, presence of all six required regions in tab order, and `:focus-visible` ring on a representative button. **met** (see Findings #1 below for an order deviation noted but not blocking)
- [x] DECISIONS.md M3 section consolidated, contradiction-free, `## M3 contract decisions (promoted for M4+)` block at the top: confirmed (`DECISIONS.md:3-33`, M3 dated entries collated underneath). **met**
- [x] README.md reflects M3 completion with diff/worktree/notifications bullets and proof pointers: `README.md:16-42`. **met**
- [x] Merged HEAD pushed to both branches with `--force-with-lease`: `origin/orch/m3-diff-worktree-notifs/m3-finalize-merge` and `origin/orch/pr-cockpit/m3-diff-worktree-notifs` both at `46cfd8b1f75d166a1a10398b5751d0376c4e4271`. **met**
- [x] All PLAN.md §10 perf budgets still green: `mutation_submit_visible_ms=0.31 < 16`, `mutation_submit_visible_online_ms=0.29 < 16`, `mutation_submit_visible_offline_ms=0.31 < 16`, `diff_scroll_frame_p95_ms=16.20 ≤ 16.7`, `diff_scroll_fps=62.07 ≥ 60`, `comrak_render_throughput_ops_per_sec=36564.14 ≥ 15000` (baseline target), `inbox_first_paint_ms=8.27 < 100`, `pr_detail_open_cold_ms=8.90 < 250`, `pr_detail_open_preloaded_ms=0.51 < 50`, `file_open_in_diff_cached_ms=0.51 < 100`. **met**

Verifier-specific mandatory checks:
- [x] AddReviewComment `Mutation` impl with `OptimismLevel::Full`: `apps/desktop/src-tauri/src/mutations/handlers/review_comments.rs:32`. GraphQL artifact at `apps/desktop/src-tauri/src/api/queries/mutations/addReviewComment.graphql`. Canonical-queries guard (`tests/canonical_queries.rs`) asserts the file lives there and that no stray `g`+`ql!` macros exist outside canonical files — passes in `cargo test --workspace`. **met**
- [x] Viewed checkbox state head_sha-scoped: read-model SQL at `apps/desktop/src-tauri/src/db/mod.rs:220-224` and `migrations/0007_diff_polish.sql:27-30` both define `is_viewed` as `CASE WHEN pf.viewed_by_account_id IS NOT NULL AND pf.viewed_at_head_sha = pr.head_sha THEN 1 ELSE 0 END`. Integration test: `tests/diff_polish_viewed_files_head_sha.rs`. **met**
- [x] Tauri asset-protocol scope tightly limited to blob-store: `tauri.conf.json` `assetProtocol.scope = ["$APPDATA/blobs/**/*"]`; `capabilities/default.json` fs:scope + fs:allow-read-file both `$APPDATA/blobs/**/*`. No open-ended scope found. **met**
- [x] Worktree watcher uses `notify` + 200–500 ms debounce + porcelain v2 + rev-list: `watcher.rs:7` (`notify::{Config, PollWatcher, RecommendedWatcher, RecursiveMode, Watcher}`); `watcher.rs:285` returns `Duration::from_millis(200 + (hasher.finish() % 301))`; `watcher.rs:313` invokes `git status --porcelain=v2 --branch`; `watcher.rs:406` invokes `git rev-list --left-right --count`. **met**
- [x] Worktree cleanup refuses user-managed worktrees and never deletes uncommitted work even with `force=true`: `worktree/mod.rs:283-294` returns `CleanupError::UserManaged` and `CleanupError::DirtyState` before any removal; `worktree/mod.rs:308-326` returns dirty branch only as snapshot (`removed: false`, `would_remove: false`) and clean branch returns `removed: false` (M3 surface intentionally never calls `git worktree remove`). Test: `tests/worktree_cleanup_safety.rs::cleanup_safety_gates_fail_closed`. **met**
- [x] Notification dedup UNIQUE constraint + INSERT OR IGNORE + post-reconcile rule engine: `migrations/0009_notifications.sql:17` defines `UNIQUE(account_id, repo_id, pr_id, event_type, actor_id, server_event_id)`; `src/notify/dedup.rs:49` uses `INSERT OR IGNORE`; `src/notify/mod.rs:90-127` consumes `SyncReconciledEvent` broadcasts (post-reconcile). Test: `tests/notifications_dedup.rs::duplicate_events_dispatch_once` (100 duplicate fires → 1 send / 1 row). **met**
- [x] Renderer does NOT call Tauri notification plugin directly: grep for `@tauri-apps/plugin-notification`, `sendNotification`, `isPermissionGranted`, `plugin-notification` across `apps/desktop/src` → 0 matches. `tests/renderer_isolation.rs` continues to enforce the broader isolation contract and passes. **met**
- [x] `pnpm corpus` weighted_mean ≤ 0.015 on 202 entries AND scorer gate = 0.015: `tools/markdown-corpus/score.mjs:12` `const GATE = 0.015`; live run printed `entries=202 weighted_mean=0.000115 gate=0.015`. (Older `score.ts` still pinned to `0.02` but is not invoked by any pnpm script — see Notes.) **met**
- [x] M2 airplane drill + m2-smoke spec still pass: both included in the green Playwright run. **met**
- [x] Accessibility screen-reader Playwright spec at `apps/desktop/playwright/a11y-pr-detail.spec.ts` and it passes: verified. **met** (see Findings #1)
- [x] Tokens still keychain-only on M3 surface: regex `(token|Authorization|ghp_|gho_|github_pat_|access_token|secret)` across `apps/desktop/src-tauri/src/worktree/`, `apps/desktop/src-tauri/src/notify/`, M3 review/viewed handlers, `apps/desktop/src-tauri/src/ipc/mod.rs`, and `apps/desktop/src/lib/components/{worktree,settings}/` → 0 credential-bearing hits. The only matches are `mention_token` strings used to parse `@user` mentions in comment bodies (`notify/triggers.rs:553-594`), which carry no auth material. **met**
- [x] DECISIONS.md M3 section consolidated + `## M3 contract decisions (promoted for M4+)` block at the top: `DECISIONS.md:3-33` enumerates the eight promoted contracts; the four sibling M3 entries follow in chronological/merge order below. **met**

Other findings (severity-ordered):
- (low) `apps/desktop/playwright/a11y-pr-detail.spec.ts` asserts tab order as `title → description → composer → timeline → right rail → merge box`, while the planner brief stated `title → description → timeline → composer → right rail → merge box`. The implementation places the composer above the timeline, so the spec matches the shipped layout but not the planner's wording. Not blocking — the spec still proves keyboard reachability, accessible naming for every interactive element, and `:focus-visible` rings. Worth noting in DECISIONS.md if the contract is to be enforced.
- (low) The legacy `tools/markdown-corpus/score.ts` still hard-codes `GATE = 0.02`. The active `pnpm corpus` script runs `score.mjs` (which is at `0.015`), so this is dead in the loop, but they should be kept in sync to avoid future confusion.
- (low) Preview-mode Playwright runs continue to emit `404 /assets/grammars/tree-sitter-rust.wasm`; tolerated by `diff-polish.spec.ts`'s allowlist as the merge handoff documented. Same env nuance the worker called out.

## Notes & suggestions
- Native OS notification dispatch and `notify` watcher timing on real filesystems are exercised by Rust integration tests (`notifications_dedup.rs`, `notifications_quiet_hours.rs`, `notifications_focus_mode.rs`, `notifications_per_repo_filters.rs`, `notifications_rule_engine.rs`, `worktree_discovery.rs`, `worktree_mapping_signals.rs`, `worktree_cleanup_safety.rs`); Playwright covers the UI integration via fixtures + the `__notif_debug__simulate_event` debug hook. Standing up a native Tauri e2e harness would let M4+ verifiers exercise the actual native notification surface and notify watcher debounce timing directly.
- The cloud-agent VM required `libwebkit2gtk-4.1-dev libgtk-3-dev libsoup-3.0-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev patchelf build-essential xvfb pkg-config` plus `playwright install --with-deps webkit chromium`; recommend running an env-setup agent at cursor.com/onboard so subsequent cloud agents skip the ~5 min apt + Playwright install.
- All artifacts and reproduction notes committed at `verify/m3/` (logs + VERIFIER_NOTES.md); branch pushed to `origin/orch/m3-diff-worktree-notifs/m3-finalize-merge`.