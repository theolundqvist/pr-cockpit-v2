{
  "$schema": "../../node_modules/.orchestrate/plan.schema.json",
  "goal": "Execute AUTONOMY_BRIEF.md to v1.0 — every milestone polished, e2e-verified, perf-green. PR Cockpit is a local-first Tauri v2 desktop app (Svelte 5 + TypeScript + Vite frontend, Rust backend with sqlx/SQLite/keyring/octocrab/comrak/ammonia/tree-sitter/notify/git2/specta) that mirrors github.com's PR UI with instant reads from SQLite and optimistic writes through a persisted mutation log. Visual parity via Primer CSS. Six milestones M1–M6 per PLAN.md and AUTONOMY_BRIEF.md; each milestone exits only when its acceptance criteria are met. Read AUTONOMY_BRIEF.md, PLAN.md, synthesis.md, DECISIONS.md in the repo for the full contract before doing any work.",
  "summary": "ship pr-cockpit v1.0 — land M1 first, then fan out M2–M6 from M1's branch",
  "rootSlug": "pr-cockpit",
  "baseBranch": "main",
  "repoUrl": "https://github.com/theolundqvist/pr-cockpit-v2",
  "selfAgentId": "bc-37db84e8-6beb-4535-ae3d-1ec364fe278b",
  "syncStateToGit": true,
  "acceptanceCriteria": [
    "All M1–M6 acceptance criteria in AUTONOMY_BRIEF.md and PLAN.md §12 are met",
    "CI green on macOS + Linux: lint, typecheck, unit tests, integration tests, headless perf benchmark, markdown-fidelity corpus regression",
    "Perf budgets in PLAN.md §10 are enforced by CI benchmarks and green at v1.0",
    "Markdown corpus regression ≤ 1% pixel-diff against github.com on 200 real comments by M6",
    "v1.0 tag with README, LICENSE, CHANGELOG, DECISIONS, PERF_REPORT in the repo"
  ],
  "tasks": [
    {
      "name": "m1-cockpit",
      "type": "subplanner",
      "model": "claude-opus-4-7-thinking-xhigh",
      "scopedGoal": "Own milestone M1 (Read-only cockpit) end-to-end per AUTONOMY_BRIEF.md and PLAN.md §12 M1. Scaffold the Tauri v2 + Svelte 5 + TS + Vite + Rust app from scratch and ship a working read-only PR cockpit on a single deliverable branch. Read AUTONOMY_BRIEF.md, PLAN.md, synthesis.md (in the repo root) and DECISIONS.md before planning; PLAN.md is the contract. Decompose into your own workers/subplanners; default to fewer, broader workers; merges are tasks. M1 deliverables, in order of fan-in: (1) Bootstrap: Tauri v2 + Svelte 5 + TS + Vite + SvelteKit static adapter; Rust crate with sqlx, keyring, octocrab (or hand-rolled reqwest), comrak, ammonia, tree-sitter wasm, notify, git2, specta, tokio, tracing, anyhow; Primer CSS (@primer/css + @primer/primitives + @primer/octicons) wired; pnpm workspace; GitHub Actions CI on ubuntu + macos covering rust fmt/clippy/test, typescript typecheck + svelte-check, unit and integration tests, a headless perf benchmark stub (criterion + a synthetic timing harness), and a markdown corpus regression stub; conventional commits + commitlint optional. (2) Auth: detect `gh auth token` for one-click import with explicit scope display; OAuth Device Flow (client_id from DECISIONS.md) as default for new users; PAT fallback; multi-account from day one; tokens in OS keychain only via `keyring`. (3) Sync engine v1: tiered focus-aware polling actor per PLAN.md §2.1 (Hot/Warm/Cool/Cold, paused after 5 min unfocus), rate-limit budgeter actor parsing X-RateLimit-* with high-priority foreground lane and back-pressure under 1000 GraphQL points / 500 REST, REST .diff fetch with Accept: application/vnd.github.v3.diff, conditional ETag/If-None-Match path, /notifications change-detector with If-Modified-Since and X-Poll-Interval. (4) Two canonical GraphQL queries: hand-written, centrally-owned `PrDetail` (title/body/timeline/threads/reviews/check summary/labels/assignees/reviewers/mergeable/headRef/baseRef) and thin `InboxRefresh` batching ~20 PR node IDs; no per-component GraphQL. (5) SQLite migrations covering tables in PLAN.md §4 (accounts, repos, repo_subscriptions, users, orgs, pull_requests, pr_labels/assignees/reviewers/projects/milestones, commits/pr_commits, comments, review_threads, reviews, check_suites/runs/annotations, pr_files, pr_patches, notifications, worktrees, pending_mutations, id_mappings, sync_cursors, rate_limit_buckets, blob_refs) + denormalized read models (pr_inbox_rows, pr_detail_summary, unread_counts, file_tree_summary) + FTS5 virtual tables; content-addressed blob store under app data dir (`blobs/<sha256-prefix>/<sha256>`) with zstd-compressed patches. (6) IPC + types: Tauri commands for queries; Tauri events for cache invalidations (`pr:123 changed`); specta-generated Rust → TS types so the renderer never opens SQLite or talks to GitHub directly. (7) Svelte 5 frontend (`$state`/`$derived`/`$effect`): inbox view served from `pr_inbox_rows` rendered from SQLite synchronously on cold start; PR detail with description, timeline, labels, reviewers, checks; side-by-side + unified diff view backed by a custom virtualized line component on @tanstack/svelte-virtual; per-line review thread anchoring using GitHub's `line`/`side`/`start_line`/`start_side` verbatim; tree-sitter wasm highlighting in a Web Worker, lazy by viewport, hard cap above 5k lines unless opted-in, token cache keyed by (language, content_hash). (8) Comrak markdown pipeline in Rust + ammonia sanitization + custom post-processors for GitHub extras (alerts, suggestion fences, @user/#123/SHA autolinks); render is cached by (content_hash, renderer_version) in the blob store; the textarea + Preview tab in the composer (not implemented in M1 but the renderer must exist for timeline rendering). (9) Markdown corpus regression: pull ~200 real comments via API on a public-repo corpus, render through comrak + post-processors, score against github.com's served HTML (use GitHub's markdown REST endpoint as oracle); CI must enforce ≤ 2% pixel-diff or equivalent text-diff metric. (10) Perf benchmark harness: enforces PLAN.md §10 budgets (inbox first paint <100 ms warm cache; PR detail open preloaded <50 ms / cold cache hit <250 ms; file open in diff cached <100 ms; diff scroll 60 fps on ≤ 5k-line diff); CI runs against WebKitGTK Linux specifically. (11) Token safety: keychain only, never SQLite, never logs (verified by a targeted test). M1 acceptance is met when the app boots offline against pre-seeded SQLite fixtures (a worker should also wire a happy-path online demo against a public repo using a CI-provided GITHUB_TOKEN if one is available, but it must not block M1 if not), the cold-start inbox paints under 100 ms on the bench, the diff renders 5k-line PRs at 60 fps in the scroll benchmark, and the markdown corpus regression passes ≤ 2%. Commit the final deliverable to your subplanner bookkeeping branch (the placeholder `orch/pr-cockpit/m1-cockpit`); the parent will use that branch as the starting ref for M2. Do not open a PR — the parent decides shipping. Read AUTONOMY_BRIEF.md §M1 and PLAN.md §12 M1 verbatim into your subplanner plan.json as `acceptanceCriteria`.",
      "acceptance": [
        "Tauri v2 + Svelte 5 + TS + Vite + Rust crate scaffold builds clean on ubuntu and macos in CI",
        "Rust toolchain config + cargo fmt + clippy --deny warnings pass; pnpm typecheck + svelte-check pass",
        "GitHub Actions CI workflow runs rust unit + integration tests, frontend unit tests, headless perf benchmark, and markdown corpus regression on every PR",
        "Auth: gh token import, OAuth Device Flow, and PAT all functional; tokens land only in OS keychain; integration tests assert no token strings in SQLite or logs",
        "Multi-account model: accounts table populated; sync state and pending mutations partition by account from day one",
        "Sync engine: tiered focus-aware poller with rate-limit budgeter; foreground bypasses back-pressure; /notifications conditional poller live; ETag/If-None-Match honored on REST",
        "Two canonical queries: PrDetail (fat) and InboxRefresh (thin batched); no per-component GraphQL queries land in M1",
        "SQLite migrations cover all PLAN.md §4 tables + denormalized read models + FTS5; content-addressed blob store wired",
        "IPC: specta-generated TS types check; webview never opens SQLite and never talks to GitHub directly (asserted in code review and via a renderer-side integration test)",
        "Svelte 5 inbox served from `pr_inbox_rows` cold-paints in <100 ms in the headless perf benchmark on a 200-row warm cache",
        "PR detail screen renders description, timeline, labels, reviewers, checks, and side-by-side + unified diff",
        "Diff viewer: tree-sitter wasm highlighting in a Web Worker, viewport-lazy; benchmark shows 60 fps scroll on a 5k-line diff",
        "Comrak + ammonia pipeline renders timeline markdown; render cached by (content_hash, renderer_version) in blob store",
        "Markdown corpus regression ≤ 2% pixel-diff (or equivalent metric) vs github.com on 200 real comments; corpus committed to repo",
        "Perf benchmark harness enforces PLAN.md §10 M1 targets and CI gates on them",
        "DECISIONS.md updated with any non-obvious calls made during M1"
      ],
      "verify": "## Setup\n- Clone the deliverable branch fresh.\n- Use the standard toolchain: rustup stable, Node 22, pnpm, Tauri v2 deps.\n- Install workspace deps and Rust deps, then run the CI workflow locally via `act` if available, or run each CI step manually.\n\n## Automated\n- `cargo fmt --check` and `cargo clippy -- -D warnings` clean.\n- `cargo test` green for backend (unit + integration).\n- `pnpm typecheck` and `pnpm svelte-check` clean.\n- `pnpm test` green for frontend unit/integration tests.\n- Markdown corpus regression suite ≤ 2% diff against github.com on 200 real comments.\n- Headless perf benchmark suite: inbox cold paint < 100 ms warm cache; PR detail preloaded < 50 ms; PR detail cold cache hit < 250 ms; file open in diff cached < 100 ms; diff scroll ≥ 60 fps on 5k-line bench.\n- Token-leak test: token strings absent from SQLite tables and from log outputs after auth + sync.\n\n## Manual\n- Boot the Tauri app pointed at the pre-seeded SQLite fixtures: inbox paints instantly, PR detail opens with timeline + diff, side-by-side and unified diff both work, tree-sitter highlighting is correct on Rust/TS/Markdown fixtures, markdown timeline matches the github.com screenshot in the fixture set.\n- Worktree feature flags off (M3 territory); merge controls off (M4 territory); composer textarea-only and read-only allowed; no writes should be wired in M1.\n\n## Gotchas\n- Don't trust JS markdown rendering; render must come from the Rust comrak pipeline.\n- Diff anchoring must use GitHub's served `.diff` and `line`/`side`/`start_line`/`start_side` verbatim — no local re-anchoring.\n- Keychain only for tokens; never SQLite or logs."
    },
    {
      "name": "verify-m1-cockpit",
      "type": "verifier",
      "verifies": "m1-cockpit",
      "model": "claude-opus-4-7",
      "scopedGoal": "Verify the m1-cockpit deliverable branch satisfies every M1 acceptance criterion from AUTONOMY_BRIEF.md §M1, PLAN.md §12 M1, and the m1-cockpit task acceptance list. Run the recipe in the inherited `verify` plan: clone the branch, install toolchains, run CI steps locally (cargo fmt/clippy/test, pnpm typecheck/test, markdown corpus regression, headless perf benchmark, token-leak test), and exercise the app manually against the pre-seeded SQLite fixtures. Confirm: cold inbox paint < 100 ms warm cache; diff scroll 60 fps on 5k-line fixture; markdown corpus regression ≤ 2%; keychain-only tokens. Emit a verifier handoff with one of `live-ui-verified` / `unit-test-verified` / `type-check-only` / `verifier-blocked` / `verifier-failed` and a per-criterion findings list."
    }
  ]
}
