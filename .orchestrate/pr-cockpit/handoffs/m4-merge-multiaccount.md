<!-- orchestrate handoff
task: m4-merge-multiaccount
branch: `orch/pr-cockpit/m4-merge-multiaccount` (= verifier tip `a95f504`, equivalent to `orch/m4-merge-multiaccount/m4-finalize`; the verifier's `verify/m4-finalize/` logs + REPORT are the final commit)
agentId: bc-daf7332b-055f-44bd-87dd-1dd5073e5578
runId: run-f05efceb-5e5c-4ccd-a97d-9a2fdad678f3
resultStatus: finished
finishedAt: 2026-05-18T06:43:10.089Z
-->

`orch/pr-cockpit/m4-merge-multiaccount` is now on origin pointing at the verifier tip. The M4 subtree is complete.

## Status
success

## Branch
`orch/pr-cockpit/m4-merge-multiaccount` (= verifier tip `a95f504`, equivalent to `orch/m4-merge-multiaccount/m4-finalize`; the verifier's `verify/m4-finalize/` logs + REPORT are the final commit)

## What my subtree did
- **m4-merge-surface** (`live-ui-verified`): `PrDetail.graphql` expanded with `mergeCommitAllowed`/`squashMergeAllowed`/`rebaseMergeAllowed`/`deleteBranchOnMerge`/`mergeQueue`/branch-protection-rule fields + `autoMergeRequest`/viewer perms; migration `0010_merge_surface.sql` (22 columns + `pr_detail_summary` view rebuild); reconcile wired through. New mutation kinds `DeleteHeadRef`, `EnqueueMergeQueue`, `DequeueMergeQueue`, `ReorderMergeQueue` (all `OptimismLevel::None`) under `mutations/handlers/merge_queue.rs`. Hardened existing `Merge` 405/409/422 conflict path. `mergeable: null` backoff `[2,5,15,45,120]s + loop 300s` emits per-tick IPC events; MockClock unit test asserts the schedule. Frontend `MergeBox.svelte` (branch-protection-aware controls, auto-merge toggle, merge queue panel, update-branch, delete-branch-after-merge), `NoOptimismButton.svelte` (confirm → spinner → `mutation:reconciled`), `MergeableBackoffMeter.svelte`. Playwright `m4-merge-surface.spec.ts` covers protection variations + sequencing + queue + update branch + backoff recording (`artifacts/m4-mergeable-backoff/tick-001..007`).
- **m4-force-push-range-diff** (`unit-test-verified`): `range_diff/` module with `LocalGitRangeDiff` (shells `git range-diff` + fetch-and-retry + `WorktreeMissingCommits` fallback) and `RestCompareRangeDiff` (REST `/compare` + patch-id-equivalent pairing + intra-line highlights via `similar`). Migration `0011_push_history.sql` (`pr_pushes` + `pr_force_push_pairs` view) with reconcile.rs ordering fix. IPC `list_pr_pushes` + `compute_range_diff` (Specta-typed, in-memory cache). New `/pr/[id]/range-diff` route + `RangeDiffView.svelte` (mode badge, commit pairs with `= ! + -` markers, expandable intra-line highlights). Force-push banner on PR header. Rust integration tests: `range_diff_local_git`, `range_diff_rest_compare`, `range_diff_fallback`, `push_history_reconcile`. Screenshots committed.
- **m4-multi-account** (`live-ui-verified`): `RateLimitBudgeter` rekeyed to `(account_id, ApiResource)`; `allow(account_id, priority)`; `snapshot()`; emits `rate_limit_pressure:<account>` + `rate_limit_bypass:<account>`. Migration `0012_multi_account_rate_limits.sql` + `account_rate_limits` view; `list_inbox(None)` aggregates across accounts with denormalized `account_login`/`account_host`. `SubmitPayload.posting_account_id` threaded through engine (rejects unknown). `AccountSwitcher.svelte`, `AccountBadge.svelte`, `RateLimitMeter.svelte`, Composer posting-identity quick-switch. Rust tests `multi_account_rate_limit`, `multi_account_inbox`, `composer_posting_identity`; Vitest `inbox-aggregated`, `Composer.quick-switch`. Playwright `m4-multi-account.spec.ts` + foreground-bypass screenshots `01-03`.
- **m4-ghe-readiness** (`live-ui-verified`): per-host endpoint derivation on `AuthAccount` (`api_base_url`/`graphql_url`); `hosts.toml` override at `PR_COCKPIT_HOSTS_TOML`; `GithubClient` routes per-account; device-flow + `gh` import gated to github.com. Settings "Add account" modal with `auth_test_endpoints(host)` validation. Rust tests `ghe_endpoint_derivation`, `ghe_round_trip` (wiremock `ghe.local` against dead loopback for dotcom — proves no api.github.com leakage), `ghe_token_storage`. Playwright `m4-ghe.spec.ts` + 3 recorded screenshots. **Also performed Step 0 chain integration**: explicit merges of `m4-merge-surface` + `m4-force-push-range-diff` into this branch, resolving conflicts in `sync/mod.rs` + `client.ts` + PR page. Combined 4-spec Playwright matrix passed.
- **m4-finalize** (`live-ui-verified`): integration sanity passed (all three upstream branches already in HEAD via m4-ghe-readiness merges). Full gate matrix green: cargo fmt/clippy/test (90 tests), pnpm typecheck/svelte-check/lint/test, Playwright `airplane + m2-smoke + m3-smoke + diff-polish + worktree + notifications + m4-merge-surface + m4-range-diff + m4-multi-account + m4-ghe` = **18/18 under xvfb**, bench (every PLAN §10 budget incl. `inbox_first_paint_ms_frontend=98≤100`, `mutation_submit_visible_ms=0.39≤16`), corpus `0.000115≤0.015`. `artifacts/m4-smoke/README.md` cross-links all six artifact folders. `DECISIONS.md` M4 section consolidated. README updated. Migrations `0010`/`0011`/`0012` present in order.
- **m4-verify** (`live-ui-verified`): independently re-ran the full gate matrix on a clean clone; spot-read the GraphQL query + migration SQL + `NoOptimismButton.svelte` + `MergeBox.svelte` delete sequencing + `run_mergeable_backoff` schedule + budgeter `HashMap<(String, ApiResource), …>` + `derive_endpoint_config` + `ghe_round_trip.rs` no-leakage assertion; ran a token-leak audit on `pending_mutations`/`accounts`/`rate_limit_buckets`/events (clean — only `Bearer` Authorization headers ever carry token bytes, gated by `set_sensitive(true)` + `ipc_token_safety` test); committed `verify/m4-finalize/{REPORT.md, *.log}` as `a95f504`. Confirmed every PLAN.md §5/§9/§12 M4 + AUTONOMY_BRIEF.md §M4 acceptance criterion met.

## Verification
live-ui-verified

(Aggregating across the subtree: 4 of 5 workers self-reported `live-ui-verified`, 1 worker `unit-test-verified`; the dedicated verifier independently ran the full matrix on the integrated branch and returned `live-ui-verified`. Verifier evidence committed at `verify/m4-finalize/REPORT.md`.)

## Notes, concerns, deviations, findings, thoughts, feedback
- **Plan adjustment mid-flight**: the original plan had all four worker tasks starting from `orch/pr-cockpit/m3-diff-worktree-notifs` as siblings. That meant downstream workers and `m4-finalize` would not see upstream work and the verifier could not validate the integrated surface. I updated `m4-ghe-readiness` to start from `m4-multi-account` and explicitly merge in `m4-merge-surface` + `m4-force-push-range-diff` as its Step 0, and chained `m4-finalize` off `m4-ghe-readiness`. This produced a clean linear deliverable history with two explicit merge commits (`35afec3` + `e603288`) and an integration commit (`38d96b1`) before each downstream worker's feature work.
- **One worker (`m4-force-push-range-diff`) returned `unit-test-verified` rather than live-ui because `m4-merge-surface.spec.ts` wasn't on its branch at the time** (sibling-branch chain issue described above). The integrated branch and the verifier subsequently ran all four M4 Playwright specs in one matrix and they were green, so this gap is fully closed at the deliverable level.
- **Three low-severity findings from the verifier** that the parent should be aware of when sequencing M5:
  - Playwright + `pnpm bench` collide on port 4173 when run concurrently (both spin up `vite preview`); the clean sequential matrix is green but CI should sequence them or pin distinct ports.
  - `db_integration::open_fixture_loads_inbox_under_timing_budget_best_effort` asserts <500 ms shared-runner ceiling and can drift past 500 ms under heavy concurrent load (passes cleanly in isolation). Consider gating on `PERF_GATE=1`.
  - `inbox_first_paint_ms_frontend` is at 98 ms vs the 100 ms hard budget — only 2 ms of headroom on the cloud-agent runner. M5 work should be mindful before adding inbox-paint cost.
- **Markdown corpus regression at 0.000115** (well under the 1.5% M3-tightened gate and well under the 1% M6 target).
- **Token safety holds across multi-account + GHE**: no token bytes in any new IPC payload, `pending_mutations`/`accounts`/`rate_limit_buckets` columns, log line, or Tauri event payload; verified by both static audit and the `ipc_token_safety` Rust test.
- **Cloud-agent VM dependency cost**: each worker re-installed `libgtk-3-dev` / `libwebkit2gtk-4.1-dev` / `libayatana-appindicator3-dev` / `librsvg2-dev` / `patchelf` / `libevent-2.1-7t64` / `libgstreamer-plugins-bad1.0-0` / `libavif16` / `libwoff1` / Playwright browsers; total wall-clock spent on apt + browser-install across the five workers was ~25–35 min. **Strongly suggest the parent run an env-setup agent at [cursor.com/onboard](https://cursor.com/onboard)** with the prompt: *"Update this repo's cloud-agent environment to preinstall Tauri Linux deps (libgtk-3-dev, libwebkit2gtk-4.1-dev, libayatana-appindicator3-dev, librsvg2-dev, patchelf, libevent-2.1-7t64, libgstreamer-plugins-bad1.0-0, libavif16, libwoff1) and Playwright browsers, plus run `pnpm install` on startup, so future agents can run `cargo clippy/test` + `pnpm bench` + xvfb Playwright without manual apt + `playwright install` steps."* M5 will see the same overhead repeated otherwise.
- One pre-existing issue from M3 (`notifications_quiet_hours::quiet_hours_suppress_os_dispatch_but_record_events` flake around UTC midnight) was not in M4's scope and was not re-tripped during M4 verification; the M3 verifier's recommended fix still applies.

## Suggested follow-ups
- **Env-setup agent** at `cursor.com/onboard` with the prompt above — this single change will save ~30 min of wall-clock per M5/M6 cloud-agent worker and eliminate a class of "missing dependency" failure handoffs.
- **CI matrix sequencing** for `pnpm bench` + Playwright (port 4173 collision); either pin distinct ports or run sequentially.
- **`PERF_GATE=1`** gating for `db_integration::open_fixture_loads_inbox_under_timing_budget_best_effort` so it only runs in dedicated perf jobs and stops tripping on shared-runner noise.
- **Provide fixture assets** `/fixture-user.png` and `src/assets/grammars/tree-sitter-rust.wasm` so Playwright specs can drop their 404 allowlists (suggested by both M3 verifier and m4-finalize worker; small but cleans up two known noise sources).
- **Re-baseline `inbox_first_paint_ms_frontend`** before/while M5 work lands — current 2 ms headroom on the cloud-agent runner is fragile. m4-finalize updated baselines for two other budgets (`inbox_first_paint_ms` 21→27 ms, `pr_detail_open_cold_ms` 22→28 ms) but did not move the frontend hard budget itself.
- **Fix the M3 quiet-hours flake** (`apps/desktop/src-tauri/tests/notifications_quiet_hours.rs`: derive `today` from window-start, not `now`) before M5 starts touching notifications, per the M3 verifier's recommended one-line fix.