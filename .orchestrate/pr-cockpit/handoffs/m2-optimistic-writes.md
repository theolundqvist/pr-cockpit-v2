<!-- orchestrate handoff
task: m2-optimistic-writes
branch: orch/pr-cockpit/m2-optimistic-writes
agentId: bc-64160f6f-aaee-42c1-b65b-277f3260fc1b
runId: run-7b215572-cdcf-424d-8d84-e3beac300bf6
resultStatus: finished
finishedAt: 2026-05-17T19:43:59.536Z
-->

M2 is complete and merged into `orch/pr-cockpit/m2-optimistic-writes`. Producing my handoff now.

## Status
success

## Branch
`orch/pr-cockpit/m2-optimistic-writes`

## What my subtree did
- **m2-mutation-engine** — Landed the M2 mutation algebra in `apps/desktop/src-tauri/src/mutations/`: `OptimismLevel { Full, Cautious, None }`, full `MutationKind` enum, `Mutation` trait with predict/apply/rollback/reconcile defaults, JSON-serializable cell-level `Patch` with inverse generation, `apply_patch` projector handling OptimisticPrediction/Rollback/ServerReconcile, engine with submit/drain/retry/discard + broadcast events + idempotency dedupe + retry classification, shared reconciler doing upserts + `body_server_adjusted` overlay + id-mapping swap + targeted refetch enqueue. Migration `0005_optimistic_writes.sql` (+ down) added `body_server_adjusted` / `server_adjusted_at` / `pending_state` overlay columns and `mutation_attempts`. Proptests (4 properties: rollback restoration, reconcile equivalence, id-mapping monotonicity, projector involution) with deterministic seeds. New `mutation_submit_visible_ms` Rust bench (0.30 ms vs 16 ms budget).
- **m2-mutation-handlers** — Implemented 27 `Mutation` impls under `mutations::handlers::*` (comments / reactions / labels / assignees / reviewers / reviews / threads / viewed_files / pr_meta / merge_controls) — the 27 covers all 25 enumerated mutation kinds from PLAN §3.1 (the count "25" in the brief vs the actual enum of 27 is a documentation drift). Each implements predict/apply/rollback/reconcile with the correct optimism level per §3.2 (Full for comments/reactions/labels/assignees/reviewers/threads/viewed/title/description/milestone, Cautious for SubmitReview/UpdateBranch/SetProject/ConvertToDraft/MarkReadyForReview, None for Merge/EnableAutoMerge/DisableAutoMerge). GraphQL mutations land under `api/queries/mutations/` (canonical-queries guard test extended). Idempotency-Key headers + `pending_mutations.idempotency_key` for client-side dedupe. Wiremock integration tests per family + a cross-family e2e test. Property tests now cover the full dispatch.
- **m2-offline-and-queue** — `NetworkMonitor` actor with injectable probe + watch channel; engine.submit() works offline (Queued event); engine.drain() on reconnect replays in submission order; unsafe (`None`) mutations stay pending with `requires_connection_confirmation`. Hard-conflict path emits `MutationEvent::HardConflict { diff }` payload (server vs predicted) — never silently dropped. `drafts` table + `db::upsert/list/delete_draft` (migration `0006_offline_queue_and_drafts.sql`). Airplane drill test (`tests/airplane_drill.rs`) runs 10 AddComment + 3 AddLabel + 3 RemoveLabel + 4 ResolveThread + 1 guarded Merge offline, reboots the engine, reconnects, asserts 20 applied/reconciled + 0 failed + 1 stuck merge (requires_connection_confirmation) + 10 id_mappings + drafts survived. Online + offline `mutation_submit_visible_ms` both 0.30 ms (budget 16 ms).
- **m2-ipc-and-frontend** — Tauri IPC commands (`submit_mutation`, `list_pending_mutations`, `retry_mutation`, `discard_mutation`, `save_draft`/`list_drafts`/`delete_draft`, `render_preview` aliasing the timeline renderer) with specta-regenerated `bindings.ts`. Mutation lifecycle events fanned out to the renderer. Svelte 5 surfaces: `Composer.svelte` (textarea + Preview tab) calling `render_preview` (the SAME `render::render_comment` Rust function as the timeline — parity by construction), `PendingAffordance`, `ServerAdjustedChip`, `InlineMutationErrorBanner`, global `SyncErrorsTray`, `HardConflictModal`. Controls wired across PR detail for every PLAN.md §3.1 kind; Merge/ClosePr/ReopenPr require confirmation modal. Offline pill + unsafe-mutation gating. Composer parity vitest (20-entry corpus, byte-equal HTML). Submit-timing vitest (< 16 ms). Playwright `airplane.spec.ts` headless WebKit drill passes. ESLint guardrails ban JS markdown libs + direct GitHub `fetch` in `apps/desktop/src/`.
- **m2-finalize** — Full local CI matrix green (cargo fmt/clippy/test, pnpm typecheck/svelte-check/lint/test, bench all PLAN §10 + new M2 budgets, corpus 1.5459 % ≤ 2 %, Playwright airplane spec). Added headless `m2-smoke.spec.ts` covering composer → label/assignee/reviewer/thread/file actions → offline queue → reconnect drain → sync tray check; captured 6 screenshots under `artifacts/m2-smoke/`. DECISIONS.md consolidated with an "M2 contract decisions (promoted for M3+)" block at the top. README marks M2 complete. Migrations through `0006_offline_queue_and_drafts.sql` confirmed in order.
- **m2-verify** — Independent rerun: `cargo fmt` clean, `cargo clippy -D warnings` clean (4m26s), `cargo test --workspace` 47/47 (24 binaries) including all proptests + airplane drill + per-family wiremock tests + canonical_queries + token_safety + ipc_token_safety + renderer_isolation; `pnpm typecheck`/`svelte-check`/`lint`/`test` (8/8) clean; `pnpm corpus` 0.015459 ≤ 0.02; `xvfb-run pnpm bench` all 14 PLAN §10 + M2 budgets met (including `mutation_submit_visible_online_ms`=0.37 / `mutation_submit_visible_offline_ms`=0.38 vs 16 ms); `xvfb-run pnpm --filter desktop test:airplane` 1/1; live WebKit walkthrough of the seeded fixture exercising optimistic comment / label / assignee / reviewer / thread / file actions + offline queue + reconnect drain + sync-errors tray; token-leak audit (zero matches under `mutations/` + M2 migrations); composer-parity traced through code (renderer side `composer-model.ts` → `commands.renderPreview` → IPC → `render_preview_impl` → `ipc_rendered_comment_html_impl` → `render::render_comment` — the same single function as the timeline). All 27 `impl Mutation for ...` blocks present; optimism levels verified per §3.2. Verifier artifacts at `verify/m2/` + `artifacts/m2-verify/` (video + trace).

## Verification
live-ui-verified

## Notes, concerns, deviations, findings, thoughts, feedback
- The brief says "25 mutation kinds" but the explicit enumeration is 27 kinds; the implementation follows the full enumerated list (every kind named in PLAN §3.1 + the brief is implemented). The verifier called this out and confirmed coverage.
- `OptimismLevel::None` is enforced for Merge / EnableAutoMerge / DisableAutoMerge (the merge-family mutations actually in the kind enum). The §3.2 "no optimism" prose also mentions squash / rebase / force-push / branch delete / dismiss review / enqueue merge queue — none of those are separate kinds in the §3.1 enumeration M2 implements (M4 territory per §12), so they don't violate the policy by their absence. The m2-ipc-and-frontend worker self-flagged this as "partial" but the verifier judged it `met` and `live-ui-verified` after re-reading PLAN §3.1.
- The hard-conflict diff modal flow is wired end-to-end (engine emits `MutationEvent::HardConflict { diff }`, IPC fans it out, Svelte modal renders), and `mutations_collaboration` + `mutations_e2e` exercise the engine + IPC paths, but the Playwright smoke can't trigger the modal because the fixture harness lacks a `__M2_DEBUG__.forceHardConflict()` hook. Low-severity gap; recommended as an M3 follow-up.
- Several workers had to apt-install Tauri Linux deps (`libwebkit2gtk-4.1-dev`, `libgtk-3-dev`, `libsoup-3.0-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`, `patchelf`, `xvfb`) + Playwright browsers on every spawn. An env-setup agent is strongly recommended; the m2-mutation-handlers + m2-finalize + m2-verify handoffs all propose concrete prompts.
- The cloud-agent VM is headless WebKit only — no real Tauri native window was booted in M2. Optimistic flow, offline queue, reconciled drain, sync-errors tray were all exercised via Playwright WebKit driving the same Svelte 5 build the Tauri preview serves, but a real-Tauri-window human pass on macOS/Linux is still owed before user-facing release. Same caveat as M1.
- AUTONOMY_BRIEF §M2 dogfood gate (one-week human use) is explicitly out-of-scope for the cloud agent and is the only §M2 exit criterion not technically achievable in this run; everything cloud-agent-achievable is met.

## Suggested follow-ups
- Run an env-setup agent at [cursor.com/onboard](https://cursor.com/onboard) to pre-bake Tauri Linux build deps (`libgtk-3-dev`, `libglib2.0-dev`, `libwebkit2gtk-4.1-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`, `patchelf`, `xvfb`) + Playwright runtime libs (`libevent-2.1-7t64`, `libgstreamer-plugins-bad1.0-0`, `libavif16`, `libwoff1`) + `pnpm install` + `playwright install --with-deps webkit chromium` so M3 workers/verifiers don't repeat that bootstrap.
- Expose a `__M2_DEBUG__.forceHardConflict()` debug hook so the hard-conflict modal can be claimed by a Playwright spec end-to-end (the engine/IPC paths are already covered).
- Move sync-errors tray trigger from PR header into the global shell topbar/status area for tighter Primer-style UX placement.
- Pin `orch/pr-cockpit/m2-optimistic-writes` (HEAD `46efc19`, merge `2d46170` containing the verifier-blessed `50ff6f9`) as M3's starting ref. Deliverable lineage: `d4e16e3 → 429cb16 → b78fda0 → df5c78d → 5d95f01 → 50ff6f9`.
- Human dogfood gate (week of comments/reviews/labels/threads as primary) remains the only §M2 exit criterion outside cloud-agent scope.